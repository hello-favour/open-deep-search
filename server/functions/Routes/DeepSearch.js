const express = require("express");

const deepSearchRoutes = express.Router();

deepSearchRoutes.get("/query", async (req, res) => {

    try {

    } catch (error) {

    }

})

module.exports = {
    deepSearchRoutes,
}